{
  "name": "audiomonitor",
  "version": "1.0.3",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start:server": "npm run start --workspace=packages/server",
    "start:dashboard": "npm run dev --workspace=packages/dashboard",
    "start:agent": "npm run dev --workspace=packages/agent",
    "test": "node tests/whitebox.test.js"
  }
}
